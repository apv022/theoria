# `theoria-core` donor audit

`/home/apv/theoria-core` was inspected read-only. The new application keeps only small
foundation-level concepts and does not copy the Vite SPA architecture or custom compiler.

| Classification | Exact source path                                            | Decision                                                                                                                                                                             |
| -------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| PORT NOW       | `/home/apv/theoria-core/src/lib/storage.ts`                  | Port the versioned IndexedDB/object-store concept. Rebuilt with raw IndexedDB, separate repository interfaces, and a compilation schema in `packages/local-store`; no source copied. |
| PORT NOW       | `/home/apv/theoria-core/src/lib/capabilities.ts`             | Keep explicit browser capability/unsupported thinking. Rebuilt as typed engine states and worker feature checks.                                                                     |
| PORT NOW       | `/home/apv/theoria-core/src/workers/compiler.worker.ts`      | Keep compilation off the main thread. Rebuilt with typed progress, cancellation, transferable data, and fatal states.                                                                |
| PORT NOW       | `/home/apv/theoria-core/src/mcf/vfs.ts`                      | Keep normalized virtual-file concepts. Rebuilt around a mounted in-memory filesystem compatible with `mcf-npm`.                                                                      |
| PORT NOW       | `/home/apv/theoria-core/src/mcf/zip.ts`                      | Keep archive-bomb and path-safety intent. Rebuilt with central-directory preflight, executable/SVG policy, bounded extraction, and deterministic output.                             |
| PORT NOW       | `/home/apv/theoria-core/src/components/QuestionView.tsx`     | Native control and feedback layout concepts informed the new controls. Behavior was rebuilt against current `mcf-npm` question semantics.                                            |
| PORT NOW       | `/home/apv/theoria-core/src/pages/ReaderPage.tsx`            | The focused outline, lesson navigation, and resume concepts informed the App Router reader. Its old types and state code were not copied.                                            |
| PORT NOW       | `/home/apv/theoria-core/src/pages/CreatePage.tsx`            | Structured tree, explicit destructive actions, source browser, and autosave concepts informed Studio. The obsolete 1.0-only model and UI code were not copied.                       |
| PORT NOW       | `/home/apv/theoria-core/src/authoring/model.ts`              | Stable IDs and deterministic source generation were useful concepts. Rebuilt for current 1.1 package kinds, source-first round trips, and real worker validation.                    |
| KEEP CONCEPT   | `/home/apv/theoria-core/src/components/CompletionStatus.tsx` | A small truthful completion indicator is useful; the new UI uses shared statuses but does not copy the component.                                                                    |
| KEEP CONCEPT   | `/home/apv/theoria-core/src/components/ScrollManager.tsx`    | Route transitions should restore reading context; App Router behavior is sufficient for the foundation.                                                                              |
| KEEP CONCEPT   | `/home/apv/theoria-core/src/pages/CompilePage.tsx`           | Import/validate/compile/history workflow informed `/compile`; UI and state management were rebuilt around the new worker contract.                                                   |
| REBUILD        | `/home/apv/theoria-core/src/App.tsx`                         | The single global navigation exposes too many workflows and uses React Router. Replaced by App Router route groups and workspace layouts.                                            |
| REBUILD        | `/home/apv/theoria-core/src/types.ts`                        | Types combine catalog, compiler, reader, enrollment, and progress. Split into framework-independent model, engine protocol, and store records.                                       |
| REBUILD        | `/home/apv/theoria-core/src/styles.css`                      | Monolithic donor styles do not express workspace layouts. Rebuilt as a responsive application foundation without a UI framework.                                                     |
| DISCARD        | `/home/apv/theoria-core/src/mcf/parser.ts`                   | Custom MCF 1.0 parser is not semantic authority. The supported `mcf-npm` 1.1 parser is used instead.                                                                                 |
| DISCARD        | `/home/apv/theoria-core/src/mcf/compiler.ts`                 | Custom compiler is not semantic authority and mixes runtime generation with old model assumptions.                                                                                   |
| KEEP CONCEPT   | `/home/apv/theoria-core/src/mcf/grading.ts`                  | Compared read-only for grading UX; current `mcf-npm` remains semantic authority and the implementation was rebuilt in `packages/reader`.                                             |
| DISCARD        | `/home/apv/theoria-core/src/mcf/render.ts`                   | Superseded by current authority and a stricter isolated preview boundary.                                                                                                            |
| DISCARD        | `/home/apv/theoria-core/src/pages/HomePage.tsx`              | Old product presentation was not ported.                                                                                                                                             |
| DISCARD        | `/home/apv/theoria-core/src/pages/DiscoverPage.tsx`          | Old catalog implementation and bundled-course assumptions were not ported.                                                                                                           |
| DISCARD        | `/home/apv/theoria-core/src/lib/catalog.ts`                  | Bundled catalog metadata conflicts with the future repository service boundary.                                                                                                      |
| KEEP CONCEPT   | `/home/apv/theoria-core/src/components/PwaStatus.tsx`        | Network-status communication informed the compact offline banner; service-worker behavior was rebuilt for the current local data model.                                              |

No donor source file was copied verbatim.
