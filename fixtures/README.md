# Fixtures

Run `pnpm fixtures:prepare` to discover, copy, index, and—when the existing read-only compiler is
available—validate representative local MCF packages. Generated content lives in `fixtures/local/`
and is intentionally ignored by Git.
